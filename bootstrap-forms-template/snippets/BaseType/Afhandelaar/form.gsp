<form:output type="text" class="fieldtype-${fieldTypeName} field-${name}" gpath="document.${gpathExpr}"  value="*{session.username}" helpTitle="${label}" helpBody="${help}" />
<input type="hidden" id="update-document.${gpathExpr}" name="update-document.${gpathExpr}" value="*{session.username}" />
