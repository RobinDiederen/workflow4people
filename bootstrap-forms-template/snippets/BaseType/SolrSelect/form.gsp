<form:solrSelect authenticationService="*{request.authenticationService}" class="${parameters.cssClass} ${field.nillable ? '' : 'required'} fieldtype-${fieldTypeName} field-${name}" gpath="document.${gpathExpr}" query="${parameters.query}" keyField="${parameters.keyField}" keyField="${parameters.valueField}" value="*{document.${gpath}}" helpTitle="${label}" helpBody="${help}" title="${alert}" />
