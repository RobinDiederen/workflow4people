name=TextArea
description="Plain text area field"
parameters {
	cssClass {
		label="CSS class"
		defaultValue="test 123"
		help="Extra CSS that is added to the form:input"
	}
	
	query {
		label="Query"
		defaultValue="select * from 123"
		help="Zo maar een query"
	}
	
	
}